# 园区光纤环网 · 拓扑工作台

改造前找出**单条链路断开即隔离站点的薄弱段（桥 / bridge）**，再通过**试接一条虚拟备纤**
验证风险是否真正消除。React + TypeScript 单页工作台，Vitest 用**删边预言机**对随机小图做
差分核对，Tarjan 桥算法在上限（200 000 站点 / 400 000 链路）下 5 秒内完成且全程迭代、不递归。

---

## 1. 输入契约

页面接受粘贴或上传一个 **JSON 对象**，仅使用普通 JSON 基础类型（对象、数组、字符串、数字、
布尔、null）。结构：

```json
{
  "sites": ["S1", "S2", "S3"],
  "links": [
    { "id": "L1", "u": "S1", "v": "S2" },
    { "id": "L2", "u": "S2", "v": "S3" }
  ]
}
```

| 字段 | 规则 |
| --- | --- |
| `sites` | **2–200 000** 个站点；编号**唯一、非空**。编号写为字符串；整数数字也会按其十进制文本接受（如 `7` 与 `"7"` 等同）。字符串编号**逐字符保留，首尾空白是编号的合法组成部分**：`"A"`、`" A "`、`"A "`、`" A"` 乃至全空白的 `" "` 都是彼此不同的合法站点。 |
| `links` | **0–400 000** 条无向链路；链路编号**唯一、非空**（字符串编号同样逐字符保留）。 |
| `links[].u / v` | 端点编号**必须存在于 `sites`**（逐字符精确匹配，不去空白、不归一化）；**禁止自环**（`u === v`）。 |
| 平行链路 | **合法**：同一对站点之间允许存在多条不同编号链路，它们彼此构成热备，均不算桥。 |
| 连通性 | **原图必须连通**（孤立站点 / 多分区将被拒绝）。 |

布尔、`null`、对象、数组、空串、`NaN`/`Infinity` 均不能作为编号。

> **字符串编号的逐字符身份**：拓扑导入接受任意非空字符串并按原字符保存，因此仅首尾空白
> 不同的编号可以在同一拓扑中合法共存。后续四类入口——**单次试接、批量方案筛选、有序备纤
> 计划复核、最低总价备纤组合**——以及自动补全回显、结果展示与错误消息，全部对编号做逐字符
> 精确匹配：绝不删除两端空白、绝不把请求静默重定向到 `trim()` 后的相似站点，也不把全空白
> 站点当作“空”。引用一个带空白但不存在的编号会原样出现在错误中（如 `"  A " 不在当前站点
> 清单中`）；数字编号继续按十进制文本（`7` 与 `"7"` 等同），普通无空白编号行为不变。

> 编号的“字节序”按其 **UTF-8 编码字节**逐字节比较（等价于 JS 字符串默认字典序，
> 实现与测试均有说明和差分验证）。注意这是字节序而非数值大小：`L10 < L2`。

### 输出语义（基线）

* 按**链路编号 UTF-8 字节序**列出**全部脆弱链路（桥）**；
* 每条给出**断开后两个连通块中较小者的站点数**（`min(子树, n − 子树)`）；
* **不输出任何站点清单**（表格仅展示编号、端点与较小侧站点数，供核对）。

### 试接（虚拟备纤）

从**现有站点**中选择**两个不同端点**试接一条虚拟备纤：

* 页面**独立**给出试接后 **仍脆弱** 的链路，以及相对基线 **已消除** 的链路；
* 试接**不改写基线**：基线对象在导入时固定，试接只读取并派生新结果；
* **非法导入**保留上次有效拓扑并显示明确错误；**非法试接**保留上次试接结果并显示明确错误。

一条新边 `(a,b)` 恰好消除桥树中 `a↔b` 路径上的全部桥——长链、大环、平行链路均得到
精确且稳定的结论（由随机差分测试锁定）。

### 批量方案筛选

在第 4 区粘贴 **1–100000 项**的 JSON 数组，每项为**仅含** `a`、`b` 两个字段的对象
（站点编号规则同单次试接）：

```json
[{"a": "S1", "b": "S9"}, {"a": "S2", "b": "S7"}]
```

* 整批先校验结构、字段、端点存在且互异；**任一项非法都按下标（0 起）报错**，
  保留上次批量结果，且不改写基线与单次试接结果；
* 全批校验通过后一次性生成结果并整体替换：按**输入下标**分页显示端点对及其
  **可消除的基线桥数量**（不展开链路清单），重复候选按原序保留；
* 空批次、额外字段、超过 100000 项均整体拒绝；
* 合法新拓扑导入后旧批量结果清空；非法导入保留既有结果。

批量计数不循环调用单次试接：Analyzer 在合法导入时基于 Tarjan 父树与桥标记构建
**只读桥前缀 + 二进制提升（binary lifting）索引**，每项经最近公共祖先（LCA）以
`prefix[a] + prefix[b] − 2·prefix[lca]` 计数，单项 O(log n)，全程迭代不递归。

### 有序备纤计划复核

在第 5 区粘贴 **1–100000 步**的 JSON 数组（结构与批量筛选完全相同，每项仅 a/b 两字段），
但**数组顺序即施工顺序**：

```json
[{"a": "S1", "b": "S9"}, {"a": "S2", "b": "S7"}]
```

* 页面按顺序给出每一步的**端点对、本步首次消除的基线桥清单、边际数、累计数与剩余数**；
  清单按**链路编号 UTF-8 字节序**、可展开核对；步骤表按输入顺序分页。
* **同一座桥只归最早覆盖它的步骤**：后续步骤的清单只含此前各步均未覆盖的桥，
  因此重复、反向、交叠或被已覆盖路径包含的后续步骤，边际数与清单均为 0。
* 各步清单**两两互斥**，其并集恰为整个计划覆盖的基线桥；累计数单调不减、
  剩余数单调不增，末步累计数 = 计划覆盖数。
* **草稿与上次成功结果独立保存**：非法计划被拒时输入草稿与上次成功结果都保留。
* 空数组、超过 100000 步、额外字段、端点不存在或两端相同均**整批拒绝**，
  错误消息指出**零起下标**，不产生部分结果；不改变基线、单次试接与批量筛选结果；
  **合法新拓扑导入后清空计划结果，非法导入继续保留**。

复核不逐步调用 `trial`、也不逐路径扫描全部链路：全批端点校验通过后，Analyzer
复用现有 DFS 父树与 LCA，维护一棵**“向父级跳转”的并查集**——

* 非桥树边的子顶点初始化时即预先指向父级（find 直接越过）；
* 每步从路径两端分别经 find 向 LCA 爬升，find 命中的自属顶点其（必为桥的）父边
  **首次归属本步**，随后把该顶点压缩到父级，之后所有步骤以近常数时间越过；
* 爬升以深度判定并在 LCA 处停止，不会收集计划路径之外的桥。

归属完成后各步清单只做一次小规模名次排序。200000 站点长链**首步全覆盖、再跟
99999 条子路径时仅首步有 199999 收益、后续全部为零**，构建与整批复核合计
约 0.5–1 秒（见 `perf.ts`，预算 5 秒）。

### 最低总价备纤组合（至多 16 条带报价候选的精确求解）

在第 6 区粘贴 **1–16 条**候选的 JSON 数组，每项为**仅含** `id`、`a`、`b`、
`price` 四个字段的对象：

```json
[
  {"id": "X1", "a": "S1", "b": "S9", "price": 1200},
  {"id": "X2", "a": "S2", "b": "S7", "price": 800}
]
```

* `id` 候选编号**全批唯一、非空**；`a`/`b` 端点规则同单次试接；
  `price` 为**非负安全整数**（允许 0，例如存量免费备纤）；
* **整批报价合计若超出 `Number.MAX_SAFE_INTEGER` 则整批拒绝**
  （此后任意子集合计才保证精确可比）；
* 空数组、超过 16 条、非对象项、缺字段、额外字段、编号重复、端点不存在/
  自环、报价为负/小数/字符串/布尔/`null`/超安全整数均**整批拒绝**，
  错误消息指出**零起下标**，保留上次求解结果，不改写基线与其他各区结果；
  合法新拓扑导入后清空求解结果，非法导入继续保留。

求解**精确枚举全部 2¹⁶ 个子集**，复用现有 Tarjan 桥检测与单条候选的桥树
路径覆盖计算（不删边、不改图），三层裁决确定**唯一**展示方案：

1. **总价最低**优先；
2. 总价相同则**条数最少**优先——因此零价但不增加覆盖的无用候选绝不会
   被擅自捎带（删去它不涨总价却少一条）；
3. 仍相同则按**候选编号 UTF-8 字节序序列**字典序取最小。

页面同时展示**所选连线与各自报价、最低总价、每条原桥由哪些所选线消除**
（候选可重复覆盖同一桥，来源全部列出，按候选编号字节序），以及
**未覆盖诊断**：

* **无桥时返回空方案**（总价 0，不选任何候选）；
* 存在任何候选都无法覆盖的桥时，**列出具体桥边且不伪造成功**
  （不给方案，`feasible=false`）。

实现上每座桥记一个 16 位“候选覆盖掩码”，用一次 **SOS 子集和 DP**
（O(16·2¹⁶)）让每个子集 O(1) 判定是否漏桥，再枚举全部子集裁决。
本功能只作规划：**不改变原图、原桥列表与既有逐条试接/批量筛选/
顺序试算结果**。

---

## 2. 本地开发

```bash
npm ci
npm run dev        # 开发服务器（Vite，默认 5173）
npm run build      # 类型检查 + 生产构建到 dist/
npm run preview    # 本地预览生产构建
npm run test       # Vitest 全量测试（含删边预言机差分）
npm run typecheck  # 仅类型检查
npx tsx src/core/perf.ts   # 上限数据（200k/400k）性能基准，断言 < 5000ms
```

测试构成：

* `src/core/utf8.test.ts` — UTF-8 字节序与真实 `TextEncoder` 编码的差分；
* `src/core/parse.test.ts` — 输入契约的各类合法 / 非法情形；
* `src/core/whitespaceIdentity.test.ts` — 字符串编号的**逐字符身份**验收：同一张连通链中
  并存 `"A"`、`" A "`、`"A "`、`" A"`、`" "` 等多组仅空白不同的站点，经四类入口逐一引用，
  核对桥集合（含删边预言机）、批量计数、有序计划首次归属、报价三层裁决与可覆盖性、
  失败按下标隔离及原结果保留，并回归数字编号与普通编号；
* `src/core/analysis.test.ts` — 生产 Tarjan 与**删边预言机**在随机小图上的逐项核对
  （桥集合、较小侧站点数、试接后仍脆弱 / 已消除），以及长链、大环、平行链路、蝴蝶结等典型拓扑；
* `src/core/batch.test.ts` — 批量方案筛选：随机连通多重小图上每项计数与
  `trial(a,b).removed.length` 及删边预言机一致、末项非法无部分结果、
  契约（空批次 / 额外字段 / 超限 / 下标报错）与较深图的 LCA 差分；
* `src/core/orderedPlan.test.ts` — 有序备纤计划：随机连通多重小图上按顺序合并
  单次试接结果作预言机（顺序归属、重复/反向/交叠/包含、互斥并集、字节序清单、
  LCA 分叉、末项非法无部分结果）与契约校验；
* `src/core/quotePlan.test.ts` — 最低总价备纤组合：随机连通多重小图（含 16 条
  候选上界）上以**删边预言机 + 独立子集穷举**逐项核对最优性与三层裁决
  （总价/条数/编号序列、零价、重复覆盖、无法覆盖诊断、空方案、大报价精确性），
  并覆盖平行边、不连通导入拒绝、零价与非法/溢出报价契约；
* `src/ui/App.test.tsx` — 导入→基线→试接→批量筛选→有序计划复核→最低总价组合
  （所选连线/费用/逐桥来源/未覆盖诊断、非法保留、新拓扑清空与不改动其他区）的端到端交互。

> 所有结论均由当前输入实时计算，**不存在固定结果 / 固定答案**。

---

## 3. Docker Compose

### 发布页面（`web`，长期运行）

宿主机端口用可覆盖的 **`WEB_PORT`** 控制（默认 `8080`）：

```bash
docker compose up --build web                 # 默认 http://localhost:8080
WEB_PORT=9000 docker compose up --build web   # 覆盖为 9000
# 或复制 .env.example 为 .env 后修改 WEB_PORT
```

镜像为多阶段构建：Node 构建静态资源后，用零依赖 Node HTTP 服务器发布（带 SPA 回退）。

### 一次性验收（`verify`）

`verify` 是**一次性服务**：依次执行类型检查、Vitest 全量测试、上限 5 秒性能基准，
**全部通过则以退出码 0 结束**，不发布端口、不重启：

```bash
docker compose --profile verify build verify
docker compose --profile verify run --rm verify
```

---

## 4. 算法与工程要点

* **桥检测**：Tarjan 无向图桥算法，`low[v] > disc[parent]` 判桥；**平行链路通过“只跳过
  通向父顶点的同一条边”正确处理**（平行的另一条作为回边降低 low 值）。
* **较小侧站点数**：DFS 同时统计子树规模 `subtree[v]`，桥断开后两侧为 `subtree[v]` 与
  `n − subtree[v]`，取较小值。
* **试接消险**：桥缩点得到桥树；新边覆盖的恰为两端点在 DFS 树上的路径桥，用父边与深度
  求 LCA 路径，O(V) 派生。
* **批量方案筛选**：合法导入时基于 Tarjan 父树与桥标记一次构建**只读桥前缀 +
  二进制提升索引**（O(n log n)，200k 站点约 15 MB 类型化数组）；每项查询经迭代式
  LCA 以 `prefix[a] + prefix[b] − 2·prefix[lca]` 计数，O(log n)，不循环调用单次试接、
  不按链路数分配缓冲、不扫描链路集合。200000 站点长链 + 100000 组查询的构建与
  整批查询合计约 1 秒（见 `perf.ts`，预算 5 秒）。
* **有序备纤计划复核**：复用同一 DFS 父树与 LCA 索引，额外维护一棵“向父级跳转”的
  并查集（`Int32Array`，长度 n）：非桥树边预先跳过，桥首次被某步路径覆盖后即
  压缩到父级；每步从两端 find 爬升至 LCA，归属总次数 ≤ 桥数，全部 find 近线性，
  不逐步调用试接、不逐路径扫描链路、不按步数分配顶点级缓冲。200000 站点长链
  首步全覆盖 + 99999 条子路径时整批复核亚秒级，构建 + 复核在 5 秒预算内。
* **最低总价备纤组合**：至多 16 条候选，复用桥检测与树路径覆盖把每座桥编码为
  16 位候选掩码；掩码为 0 的桥直接作为“无法覆盖”诊断（不伪造成功）。一次
  SOS 子集和 DP（O(16·2¹⁶)，约 100 万次运算）把“某子集是否漏桥”降为 O(1)，
  再枚举全部子集按**总价 → 条数 → 编号 UTF-8 字节序序列**三层裁决取唯一最优；
  报价合计在解析层即保证为安全整数，子集总价用 `Float64Array` 递推仍精确。
  200000 站点长链 + 16 条分片候选（树路径标记 16n 步）整批求解约 0.1 秒
  （见 `perf.ts`，预算 5 秒）。
* **不撑爆调用栈**：Tarjan 与连通性 / BFS 全部为**显式栈 / 显式队列迭代实现**，
  200 000 个站点的纯长链也不会递归（已实测）。
* **性能与内存**：邻接表、DFS 缓冲均使用紧凑类型化数组（`Int32Array` / `Uint8Array`），
  避免约 80 万邻接记录装箱；上限数据解析 + 基线 + 试接合计约 2 秒（见 `perf.ts`，预算 5 秒）。

## 目录结构

```
src/
  core/            纯逻辑（无 DOM 依赖，可独立测试）
    types.ts       数据类型与错误
    utf8.ts        UTF-8 字节序比较
    parse.ts       导入契约解析与校验（拓扑 + 批量方案 + 有序计划 + 备纤报价，含连通性）
    analysis.ts    Tarjan 桥分析器 + 试接 + 批量筛选 + 有序计划复核 + 最低总价组合（迭代实现）
    oracle.ts      删边预言机（仅测试用朴素参考实现）
    *.test.ts      Vitest 差分 / 契约 / 空白编号逐字符身份测试
    perf.ts        上限性能基准（含 200k 长链 + 100k 批量查询 + 100k 步有序计划 + 16 条报价候选）
  ui/              React 工作台（导入、基线复核、试接核对、批量方案筛选、有序计划复核、最低总价组合、分页表格）
docker/            零依赖静态服务器
Dockerfile         web 镜像
Dockerfile.verify  一次性验收镜像
docker-compose.yml web + verify(profile)
```
